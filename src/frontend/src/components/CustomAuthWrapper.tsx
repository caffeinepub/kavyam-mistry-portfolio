// No longer used - portfolio is now public.
import type { ReactNode } from "react";

export default function CustomAuthWrapper({
  children,
}: { children: ReactNode }) {
  return <>{children}</>;
}
